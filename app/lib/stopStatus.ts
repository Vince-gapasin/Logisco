// Kept for existing imports: the definitions now live in app/lib/enums.ts
// alongside every other database enum.
export { STOP_STATUS, isStopDelivered, isStopFailed } from "@/app/lib/enums";
export type { StopStatus } from "@/app/lib/enums";
